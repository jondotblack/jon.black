import styled from '@emotion/styled';

export const Input = styled.input`
  font-size: 18px;
  line-height: 24px;
  color: #40413b;
  background: transparent;
  border: 0;

  &:focus {
    outline: none;
  }
`;
